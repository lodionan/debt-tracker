# Database Configuration
spring.datasource.url=${JDBC_DATABASE_URL:jdbc:mysql://localhost:3306/debt_tracker?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC}
spring.datasource.username=${JDBC_DATABASE_USERNAME:payolemo}
spring.datasource.password=${JDBC_DATABASE_PASSWORD:huaraide.123}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Server Configuration
server.port=8080

# JWT Configuration
jwt.secret=mySecretKey
jwt.expiration=86400000

# CLIP Payment Gateway Configuration
clip.api.key=${CLIP_API_KEY:your_clip_api_key}
clip.api.secret=${CLIP_API_SECRET:your_clip_api_secret}
clip.merchant.id=${CLIP_MERCHANT_ID:your_merchant_id}

# CORS Configuration
cors.allowed-origins=http://localhost:3000,http://localhost:3001
cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
cors.allowed-headers=*
cors.allow-credentials=true

# Email Configuration (for development - replace with real SMTP settings)
spring.mail.host=localhost
spring.mail.port=587
spring.mail.username=
spring.mail.password=
spring.mail.properties.mail.smtp.auth=false
spring.mail.properties.mail.smtp.starttls.enable=false

# Stripe Configuration (replace with real keys in production)
stripe.secret.key=sk_test_dummy_key_for_development
stripe.publishable.key=pk_test_dummy_key_for_development

# Scheduling
spring.task.scheduling.enabled=true

# Logging
logging.level.com.businessrank=DEBUG
logging.level.org.springframework.security=DEBUG